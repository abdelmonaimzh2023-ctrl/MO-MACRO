<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:label="MO MACRO"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoMacro">

        <activity
            android:name="com.monaim.studio.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.monaim.studio.ui.SettingsActivity"
            android:label="Settings"
            android:configChanges="orientation|screenSize" />

        <service
            android:name="com.monaim.studio.service.MacroAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name="com.monaim.studio.overlay.FloatingWidgetService"
            android:exported="false" />

    </application>
</manifest>
